// .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  rust:
    name: Rust (fmt · clippy · check · test)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry & build
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "omega-workspace"
          cache-on-failure: true

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy (deny warnings)
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Check
        run: cargo check --workspace

      - name: Test
        run: cargo test --workspace

  contracts:
    name: Contracts (Foundry)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Build & test contracts
        working-directory: contracts
        run: |
          forge build
          forge test

  # Optional future expansion points (commented, ready to enable):
  # certora:
  #   name: Formal (Certora)
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/')
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Certora
  #       run: |
  #         # requires CERTORAKEY secret
  #         certoraRun certora/specs/Orchestrator.spec --msg "OmegaEngine Orchestrator"
  #         certoraRun certora/specs/Vault.spec --msg "OmegaEngine Vault"